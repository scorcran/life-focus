export default function InterruptsPage() {
  return (
    <section aria-labelledby="interrupts-heading">
      <h1 id="interrupts-heading" className="headline-lg" style={{ margin: 0 }}>
        Interrupts
      </h1>
      <p className="body-lg" style={{ color: 'var(--light-on-surface-variant)' }}>
        What are the tradeoffs if I accept this?
      </p>
    </section>
  );
}
