---
scope: Life Focus Intelligence — MVP wedge through v1.0
purpose: build substrate for solo founder-builder
altitude: product/initiative (PRD → features)
updated: 2026-07-12T15:52
---

- (direction) Peer-session research received (AI layer, verified against platform.claude.com docs): current Claude lineup = Fable 5 / Opus 4.8-4.6 / Sonnet 5-4.6 / Haiku 4.5; newer models (Fable 5, Opus 4.7+, Sonnet 5) use adaptive thinking with effort parameter — budget_tokens rejected (400); two-tier pattern (cheap model extracts structured data, strong model reasons/plans) is established practitioner guidance, Haiku ~10-20x cheaper than Opus per token. Feeds OQ-1 decision; current pricing still pending from own research agent.
- (decision) Sean's coaching answers: (1) stack open — Claude to recommend starter; (2) ops: managed services OK if cheap, home unraid server available but prefers not to host the DB; (3) system heart = DETERMINISTIC planning engine, LLM interprets content feeding it (not LLM-does-the-planning); (4) external LLM may access ALL context (both work+personal) under no-training API terms — resolves OQ-9's hardest form: SEC-2 boundary governs third-party-visible outputs, not the trusted model API path.
- (decision) AD candidates decided by Sean: (1) PARADIGM = hexagonal (ports & adapters) modular monolith — pure deterministic planner core, no I/O or LLM calls inside; LLM/connectors/UI are adapters producing typed confidence-tagged assertions; adapters depend on core, never reverse. (2) STATE = event-ledger-lite — append-only command/event tables + projected current state; undo = compensating command; Commitment Ledger, plan history, and SEC-2 cross-context audit are one mechanism. (3) AI LAYER = two-tier via single gateway module: claude-haiku-4-5 (batched+prompt-cached) extraction, claude-sonnet-5 reasoning/explanation; per-call cost logging; model routing by config. Stack question open: Sean asked frontend/backend split — answered: TS monorepo, framework-free hexagonal core packages, two thin hosts (Next.js web + bare-Node pg-boss worker), no separate backend service.
- (decision) STACK locked by Sean: TypeScript end-to-end monorepo; Next.js 16 web host; framework-free TS core packages (planner/ledger/interpretation/connectors); bare-Node worker host with pg-boss; Drizzle ORM; Better Auth; Anthropic TS SDK behind gateway; Docker Compose on unraid; managed Postgres (Neon-vs-Supabase decision in flight). Calibration note: Sean is at technical-leadership altitude, building heavily with AI assistance — strengthens TS case (types as agent guardrail across UI/core seam).
- (decision) DB locked by Sean: Supabase free tier as plain Postgres (no Supabase Auth/SDK/realtime — plain connection string via Drizzle, DB swappable). Rationale: pg-boss polling keeps DB always-awake, defeating Neon scale-to-zero economics while making Supabase 7-day idle-pause a non-issue. Conditions: platform-agnostic usage is a rule (AD candidate); nightly pg_dump via pg-boss to unraid array covers NFR-3 backup story.
- (decision) Final coaching decisions by Sean: (1) IDENTITY: one app user; source identities = (provider, account, context) with context assigned at connect time, never inferred; Person merge across contexts is USER-INITIATED ONLY, merge link audited, merged Person becomes joint-context — resolves OQ-3. (2) CONTEXT TAGS: work/personal/joint; joint legal only for planning-layer artifacts; all third-party-visible output passes broker filter regardless of tag — closes OQ-9. (3) DEV ENV: local Postgres container via docker compose, major version pinned to Supabase prod. Conventions accepted: adapters→core only; single LLM gate (interpretation adapter, typed assertions w/ confidence+provenance); planner consumes only typed facts, never raw content; secrets .env on unraid; sync health in-app (FR-62), no external monitoring at MVP.
- (change) Reconciliation fixes applied: AD-2 no-plan-cache rule; AD-3 Anthropic-egress-vs-SEC-2 sentence (closes OQ-1 fully); AD-5 MVP broker scope = tag+audit; new AD-10 assertion-conflict authority order in core; conventions rows for accessibility (NFR-7) and mobile/PWA baseline (NFR-9/AC-6).
- (change) Reviewer-gate fixes applied: ContextSnapshot single zod schema (AD-2); command/event schemas + all projections owned by ledger, undo = compensating forward event only (AD-4); LLM calls execute only in worker via jobs, web appends commands (AD-3, deployment diagram updated); broker filters everything leaving the authenticated surface incl. notifications (AD-5); intra-core dependency direction (AD-1); conventions added: web mutations via server actions, pg-boss failure/dead-letter/sync-health policy, Vitest test architecture, secrets via compose env_file; stack: TS 7.x, Node 24 LTS (web-verified), UUIDv7 app-side note (Supabase=PG17). Rubric walker's 'Next.js 16.2 not real' claim REJECTED — version reviewer web-verified 16.2.10 current stable; training-data speculation lost to live verification.
- (event) Reviewer gate complete: lint 0 findings (x2); rubric + versions + adversarial lenses run; all critical/high findings resolved in spine; mediums/lows preserved in reviews/ folder. No open blockers; remaining vagueness (no-training compliance mechanism, 'materially uncertain' threshold) deliberately left to build time.
- (event) spine finalized
